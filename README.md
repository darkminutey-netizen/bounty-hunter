Modify the 'Write the config' section to remove 'polling_interval: 5' if the script is intended to be triggered by launchd, or update the 'Background Monitor' section to clarify that the script should be run as a persistent service if it handles its own timing logic.